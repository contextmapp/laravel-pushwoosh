language: php

php:
- 7.0
- 7.1
- 7.2

sudo: false

cache:
  directories:
  - $HOME/.composer/cache/files

install: composer install --prefer-dist --no-interaction --no-progress --no-suggest

script: vendor/bin/phpunit --coverage-text
